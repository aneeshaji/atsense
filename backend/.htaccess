<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all traffic into the Laravel public folder
    RewriteCond %{REQUEST_URI} !^/api/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
